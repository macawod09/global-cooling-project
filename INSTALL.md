# 📋 Инструкция по установке и запуску

## Пошаговая инструкция для GitHub Codespaces

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New"** (создать новый репозиторий)
3. Дайте имя репозиторию, например: `global-cooling-website`
4. Выберите **Public** или **Private**
5. Нажмите **"Create repository"**

### Шаг 2: Загрузка файлов в репозиторий

#### Способ A: Через веб-интерфейс GitHub

1. На странице вашего нового репозитория нажмите **"uploading an existing file"**
2. Перетащите все файлы проекта:
   - `index.html`
   - `styles.css`
   - `script.js`
   - `README.md`
   - `package.json`
   - `.gitignore`
3. Нажмите **"Commit changes"**

#### Способ B: Через Git командную строку

```bash
git clone https://github.com/ваш-username/global-cooling-website.git
cd global-cooling-website

# Скопируйте все файлы проекта в эту папку

git add .
git commit -m "Initial commit: Global Cooling Systems website"
git push origin main
```

### Шаг 3: Запуск в GitHub Codespaces

1. Откройте ваш репозиторий на GitHub
2. Нажмите зелёную кнопку **"Code"**
3. Выберите вкладку **"Codespaces"**
4. Нажмите **"Create codespace on main"**

Codespaces начнёт загрузку (это займёт 1-2 минуты).

### Шаг 4: Запуск веб-сайта

После открытия Codespaces у вас есть несколько вариантов:

#### ⚡ Вариант 1: Быстрый запуск (рекомендуется)

1. Откройте терминал в Codespaces (Terminal → New Terminal)
2. Выполните команду:

```bash
python3 -m http.server 8000
```

3. Codespaces покажет уведомление с кнопкой **"Open in Browser"**
4. Нажмите на эту кнопку - сайт откроется!

#### 🚀 Вариант 2: Использование npm скрипта

```bash
npm start
```

Это запустит Python HTTP сервер на порту 8000.

#### 🔥 Вариант 3: Live Server (с автообновлением)

1. Откройте `index.html` в редакторе
2. Нажмите правой кнопкой мыши на файл
3. Выберите **"Open with Live Server"**

Если у вас нет Live Server:
- Нажмите `Ctrl+Shift+X` (Extensions)
- Найдите "Live Server" от Ritwick Dey
- Нажмите "Install"

#### 🌐 Вариант 4: Node.js http-server

```bash
npx http-server -p 8000 -o
```

Флаг `-o` автоматически откроет браузер.

### Шаг 5: Просмотр сайта

После запуска любым из способов выше:

1. Codespaces покажет уведомление **"Your application running on port 8000 is available"**
2. Нажмите **"Open in Browser"** или **"Open in Preview"**
3. Сайт загрузится!

Или найдите порт вручную:
- Откройте панель **"PORTS"** внизу Codespaces
- Найдите строку с портом **8000**
- Нажмите на иконку глобуса 🌐 для открытия в браузере

## 🔧 Редактирование и разработка

### Структура файлов

```
global-cooling-project/
│
├── index.html          ← Основная HTML-разметка
├── styles.css          ← Все стили (цвета, шрифты, анимации)
├── script.js           ← JavaScript (формы, меню, анимации)
├── README.md           ← Документация
├── INSTALL.md          ← Эта инструкция
├── package.json        ← Конфигурация проекта
└── .gitignore          ← Игнорируемые файлы
```

### Как редактировать:

1. **Изменить цвета**:
   - Откройте `styles.css`
   - Найдите `:root` (первые строки)
   - Измените значения переменных:
   ```css
   --primary-navy: #0A1D37;
   --corporate-blue: #2196F3;
   --light-blue: #E3F2FD;
   ```

2. **Изменить текст**:
   - Откройте `index.html`
   - Найдите нужный текст и измените
   - Сохраните файл
   - Страница обновится автоматически (если используете Live Server)

3. **Изменить изображения**:
   - Замените URL в `index.html`:
   ```html
   <img src="https://ваша-ссылка.jpg" alt="описание" />
   ```

4. **Добавить функциональность**:
   - Откройте `script.js`
   - Добавьте свой JavaScript код

## 📱 Тестирование на разных устройствах

### В браузере:

1. Откройте DevTools: `F12` или `Ctrl+Shift+I`
2. Нажмите иконку **"Toggle device toolbar"** (или `Ctrl+Shift+M`)
3. Выберите устройство: iPhone, iPad, Android и т.д.

## 🚨 Решение проблем

### Проблема: Сайт не открывается

**Решение**:
1. Проверьте, что сервер запущен (в терминале не должно быть ошибок)
2. Откройте панель PORTS
3. Убедитесь, что порт 8000 виден как "Forwarded"
4. Попробуйте другой порт: `python3 -m http.server 8080`

### Проблема: Стили не загружаются

**Решение**:
1. Проверьте, что `styles.css` в той же папке, что и `index.html`
2. Откройте DevTools → Console
3. Проверьте наличие ошибок 404

### Проблема: JavaScript не работает

**Решение**:
1. Откройте DevTools → Console
2. Проверьте наличие ошибок
3. Убедитесь, что `script.js` подключен в конце `index.html`

## 💡 Полезные команды

```bash
# Остановить сервер
Ctrl+C

# Запустить на другом порту
python3 -m http.server 3000

# Посмотреть все процессы
ps aux | grep python

# Убить процесс по порту 8000
lsof -ti:8000 | xargs kill
```

## 🎓 Дополнительные ресурсы

- [GitHub Codespaces документация](https://docs.github.com/en/codespaces)
- [MDN Web Docs](https://developer.mozilla.org/) - справочник по HTML, CSS, JS
- [CSS-Tricks](https://css-tricks.com/) - статьи по CSS

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи в терминале
3. Проверьте консоль браузера (F12)

---

**Готово! Теперь вы можете разрабатывать и тестировать сайт прямо в браузере!** 🎉